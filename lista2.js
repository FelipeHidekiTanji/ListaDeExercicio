// Criar um progama que mostre se um número é positivo ou negativo.

let n1 = -80;
if (n1 >=0) {
    console.log(`O número ${n1} é positivo.`);
}   else {
    console.log(`O número ${n1} é negativo.`);
}

// Criar um progama que mostre se um ano é bissexto.

let ano = 2020;
if (ano % 4 === 0) {
    console.log(`O ano de ${ano} é bissexto.`)
}   else {
    console.log(`O ano de ${ano} não é bissexto.`)
}

// Criar um progama que mostre uma senha e a mensagem acesso permitido.

let senha = 1234;
if (senha === 1234) {
    console.log(`Acesso permitido`)
}   else {
    console.log(`Acesso negado`)
}

// Criar um progama que mostre um desconto de 5% para compras maiores que R$100.

// Criar um progama que classifique uma pessoa como criança, adolecente ou adulto de acordo com a idade.

// criar um progama que mostre qual tipo de tranangulo é diacordo com seus lados (Equilatero, Isóceles ou Escalneo).

// Criar um progama que mostre conceitos A B C ou D e acordo com as notas: >9.5 A; Entre 9.4 e 8.5 B; Entre 8.4 e 7.0 C;